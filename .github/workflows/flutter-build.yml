name: Flutter Build Platform Shared

on:
  workflow_call:
    inputs:
      upload-artifact:
        description: 'Upload artifact'
        required: false
        default: false
        type: boolean

env:
  CARGO_EXPAND_VERSION: "1.0.95"
  FLUTTER_VERSION: "3.22.3"
  FLUTTER_RUST_BRIDGE_VERSION: "1.80.1"
  RUST_VERSION: "1.75"

jobs:
  build:
    name: ${{ matrix.job.target }} (${{ matrix.job.os }})
    runs-on: ${{ matrix.job.os }}
    strategy:
      fail-fast: false
      matrix:
        job:
          - { target: x86_64-pc-windows-msvc, os: windows-2022 }
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Export GitHub Actions cache environment variables
        uses: actions/github-script@v6
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Install flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.RUST_VERSION }}
          targets: ${{ matrix.job.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: flutter-build-${{ matrix.job.os }}

      - name: Install vcpkg dependencies
        run: |
          vcpkg install --triplet x64-windows
        shell: pwsh

      - name: Build rustdesk
        run: |
          python3 ./build.py --portable --hwcodec --flutter --vram --skip-portable-pack
          mv ./flutter/build/windows/x64/runner/Release ./rustdesk
        shell: pwsh

      - name: Force Upload Custom Windows Client
        uses: actions/upload-artifact@v4
        with:
          name: Moje-Vlastni-Podpora-Windows
          path: rustdesk/rustdesk.exe
      - name: Konecne Ulozeni Windows Klienta
        uses: actions/upload-artifact@v4
        with:
          name: Moje-Vlastni-Podpora-Windows
          path: rustdesk/
